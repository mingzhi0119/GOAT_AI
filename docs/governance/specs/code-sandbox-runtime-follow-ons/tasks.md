# Code Sandbox Runtime Follow-ons Tasks

Status: draft

This is a non-canonical working artifact.

## Task list

- [x] Create the required spec, plan, and task files.
- [x] Add the Task 4 decision package entrypoint and governance allow-list
  updates needed for the spec pilot.
- [x] Land the process-local supervisor seam plus running cancellation and
  fail-closed startup recovery.
- [x] Add workspace manifest/runtime metadata hints and narrow the public
  network-policy contract to shipped behavior only.
- [x] Add happy-path, boundary, and contract proof for running cancellation,
  restart recovery, and disabled-only network policy semantics.
- [x] Run the minimum backend, contract, frontend, and governance validation
  matrix for the touched files.
- [x] Move only the landed runtime-control facts into `PROJECT_STATUS.md`,
  narrow the remaining blockers in `ROADMAP.md`, and append the Codex log entry.
