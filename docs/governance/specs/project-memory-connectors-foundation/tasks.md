# Project Memory And Connectors Foundation Tasks

Status: draft

This is a non-canonical working artifact.

## Task list

- [x] Create the required spec, plan, and task files.
- [x] Add the Task 2 decision package entrypoint and governance allow-list
  updates needed for the spec pilot.
- [x] Land caller-scoped source inventory for read-only project memory and
  connector bindings.
- [x] Wire bounded runtime execution for `project_memory` and visible connector
  bindings without widening write capabilities.
- [x] Add happy-path, concealment, and missing-project contract proof.
- [x] Run the minimum backend, contract, frontend, and governance validation
  matrix for the touched files.
- [x] Move only the landed read-only facts into `PROJECT_STATUS.md`, remove the
  finished slice from `ROADMAP.md`, and append the Codex log entry.
